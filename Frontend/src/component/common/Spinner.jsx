import { Oval } from "react-loader-spinner";

const Spinner = () => {
  return (
    <div className="h-screen flex items-center justify-center">
      <Oval height={40} width={40} color="#3b82f6" />
    </div>
  );
};

export default Spinner;
